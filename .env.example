# Live external data sources (USGS + Open-Meteo).
# Defaults to false so a fresh clone never silently calls external APIs.
LIVE_DATA_ENABLED=false

# Polling cadence — never poll faster than these values.
POLL_INTERVAL_EARTHQUAKE_MIN=5
POLL_INTERVAL_WEATHER_HOURS=3
